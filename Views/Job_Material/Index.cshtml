@model IEnumerable<CLDV_Framework.Models.Job_Material>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Quantity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Job.JobCardID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Material.Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Quantity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Job.JobCardID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Material.Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.JobMaterialID }) |
            @Html.ActionLink("Details", "Details", new { id=item.JobMaterialID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.JobMaterialID })
        </td>
    </tr>
}

</table>
