@model IEnumerable<CLDV_Framework.Models.Job>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
           Job Card No.
        </th>
        <th>
           Customer Name
        </th>
        <th>
            Job Type
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.JobCardID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Customer.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.JobType.JobType1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.JobCardID }) |
            @Html.ActionLink("Details", "Details", new { id=item.JobCardID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.JobCardID })
        </td>
    </tr>
}

</table>
